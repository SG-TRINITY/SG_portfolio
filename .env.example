# Optional: live GitHub contribution graph on the homepage.
# Create a classic PAT: GitHub → Settings → Developer settings → Personal access tokens
# Enable scope: read:user
# On Vercel: Project → Settings → Environment Variables → add GITHUB_TOKEN

GITHUB_TOKEN=
